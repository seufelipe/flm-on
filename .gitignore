# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# runtime cache and weekly-batch staging — data/showtimes.json (published data) and the
# curated override / editorial files below are committed, everything else here is not
/data/*.json
!/data/showtimes.json
!/data/upcoming.json
!/data/title-overrides.json
!/data/letterboxd-overrides.json
!/data/film-labels.json
!/data/hidden-films.json
!/data/language-overrides.json
!/data/director-overrides.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
